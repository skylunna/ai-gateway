version: 2

project_name: luner

builds:
  - id: luner
    main: ./cmd/luner
    binary: luner
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X github.com/skylunna/luner/cmd/luner.Version={{.Version}}
      - -X github.com/skylunna/luner/cmd/luner.commit={{.Commit}}
      - -X github.com/skylunna/luner/cmd/luner.buildDate={{.Date}}

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE
      - config/config.example.yaml

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ .Tag }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
      - Merge pull request
      - Merge branch

# 重点修复：dockers 不支持多架构数组，必须拆分成多个条目
dockers:
  - id: luner-amd64
    use: buildx
    dockerfile: Dockerfile.goreleaser
    goos: linux
    goarch: amd64  # 字符串，不是数组 ✅
    image_templates:
      - "ghcr.io/skylunna/luner:{{ .Version }}-amd64"
    build_flag_templates:
      - "--pull"
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.title={{ .ProjectName }}"
      - "--label=org.opencontainers.image.description={{ .ProjectName }} - LLM API Gateway"
      - "--label=org.opencontainers.image.url=https://github.com/skylunna/luner"
      - "--label=org.opencontainers.image.source=https://github.com/skylunna/luner"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.created={{ .Date }}"
      - "--label=org.opencontainers.image.revision={{ .FullCommit }}"
    skip_push: false

  - id: luner-arm64
    use: buildx
    dockerfile: Dockerfile.goreleaser
    goos: linux
    goarch: arm64  # 字符串，不是数组 ✅
    image_templates:
      - "ghcr.io/skylunna/luner:{{ .Version }}-arm64"
    build_flag_templates:
      - "--pull"
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.title={{ .ProjectName }}"
      - "--label=org.opencontainers.image.description={{ .ProjectName }} - LLM API Gateway"
      - "--label=org.opencontainers.image.url=https://github.com/skylunna/luner"
      - "--label=org.opencontainers.image.source=https://github.com/skylunna/luner"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.created={{ .Date }}"
      - "--label=org.opencontainers.image.revision={{ .FullCommit }}"
    skip_push: false

docker_manifests:
  - name_template: "ghcr.io/skylunna/luner:{{ .Version }}"
    image_templates:
      - "ghcr.io/skylunna/luner:{{ .Version }}-amd64"
      - "ghcr.io/skylunna/luner:{{ .Version }}-arm64"

  - name_template: "ghcr.io/skylunna/luner:latest"
    image_templates:
      - "ghcr.io/skylunna/luner:{{ .Version }}-amd64"
      - "ghcr.io/skylunna/luner:{{ .Version }}-arm64"
    skip_push: false